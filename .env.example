# Optional — cross-device sync (src/sync/). Leave unset to run fully offline.
# Get these from the Supabase dashboard: Project Settings > Data API (URL) and
# Project Settings > API Keys (anon/public key — never the service_role key).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
